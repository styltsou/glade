## No Specs Required

This change removes UI components without modifying any system capabilities:

- **New Capabilities**: None - this is a removal, not an addition
- **Modified Capabilities**: None - removing a UI component doesn't change requirements

The sidebar search is purely a UI feature. The underlying search functionality remains unchanged and is still accessible via the command palette.
